gradient_boosting:
  weak_learner: "neural_network"
  upsilon: 0.8
  learning_rate: 0.01
  patience: 3
  max_iter: 20

decision_tree:
  init_params:
    max_depth: 5
  fit_params: null

neural_network:
  init_params: null
  fit_params:
    learning_rate: 0.01
    weight_decay: 0.001
    epochs: 20
    patience: 3